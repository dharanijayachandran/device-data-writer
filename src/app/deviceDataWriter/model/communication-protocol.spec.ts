import { CommunicationProtocol } from './communication-protocol';

describe('CommunicationProtocol', () => {
  it('should create an instance', () => {
    expect(new CommunicationProtocol()).toBeTruthy();
  });
});
